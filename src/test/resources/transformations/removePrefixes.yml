unclassified:
  awsCredentialsProvider:
    client:
      endpointUrl: ${AWS_ENDPOINT_URL}
      region: ${AWS_REGION}
    transformations:
      name:
        removePrefixes:
          prefixes:
            - "staging-"
            - "production-"