unclassified:
  awsCredentialsProvider:
    client:
      endpointUrl: ${AWS_ENDPOINT_URL}
      region: ${AWS_REGION}
    listSecrets:
      filters:
        - key: tag-key
          values:
            - product
        - key: tag-value
          values:
            - foo