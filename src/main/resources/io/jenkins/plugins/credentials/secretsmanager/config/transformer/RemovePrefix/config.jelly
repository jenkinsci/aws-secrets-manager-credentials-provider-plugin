<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
    <f:entry title="${%prefix}">
        <f:textbox field="prefix" />
    </f:entry>
    <f:validateButton
            title="${%testTransformation}"
            progress="${%testing}"
            method="testTransformation"
            with="prefix" />
    <f:block>
        <div class="info">${%healthWarning}</div>
    </f:block>
</j:jelly>